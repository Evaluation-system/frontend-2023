import { FC } from "react";

const Footer: FC = () => {
  return (
    <section className="flex mt-80 bg-primary h-20 opacity-50">
      <div className="flex mx-auto px-20">Footer</div>
    </section>
  );
};

export default Footer;
